// This is a placeholder screen for the center tab button
// The actual action is handled in the tab bar and redirects to /create-quote
import { View } from 'react-native';

export default function CreatePlaceholder() {
  return <View />;
}
